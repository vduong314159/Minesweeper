function blank_hit(Bx,By,size,board)
global a0 a1 a2 a3 a4 a5 a6 a7 a8 aflag amine aempty

pause(2)

if Bx>=1 & Bx<=size & By>=1 & By<=size & board{By,Bx}{1,1}==0
    image_creation(board{By,Bx}{1,2},Bx,By,size);
    board{By,Bx}{1,1}=1;
end
    if board{By,Bx}{1,3}~=0
        return
    end
    
end
return